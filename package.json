{
  "name": "manage-sys",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --mode base",
    "prod": "vite --host --mode prod",
    "build:base": "vue-tsc && vite build",
    "build": "vue-tsc && vite build --mode prod",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "axios": "lastest",
    "element-plus": "^2.7.8",
    "pinia": "lastest",
    "pinia-plugin-persistedstate": "lastest",
    "sass": "lastest",
    "view-ui-plus": "lastest",
    "vue": "lastest",
    "vue-draggable-plus": "^0.5.3",
    "vue-router": "lastest"
  },
  "devDependencies": {
    "@types/node": "lastest",
    "@vitejs/plugin-vue": "lastest",
    "typescript": "lastest",
    "vite": "lastest",
    "vue-tsc": "lastest"
  }
}
